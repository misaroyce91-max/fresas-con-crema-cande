import type {Metadata} from 'next'
export const metadata:Metadata={title:'Cande Repartidor',description:'Aplicación de entregas de Fresas con Crema Cande',manifest:'/driver-manifest.webmanifest'}
export default function DriverLayout({children}:{children:React.ReactNode}){return children}
